# 盲测题库（定级元能力验证 · 5 单）

> 目的：验证架构师在**陌生仓库/陌生语言**下的 S/M/L 定级是否靠谱。
> 与 L 级题库的区别：这些题目不给架构师任何"这题很难"的暗示，由它自主定级、自主拆卡。
> 工具链（全部全路径调用，管道环境 PATH 没有它们）：
> - Python: `C:\Python314\python.exe`（pytest 已装）
> - Node: `D:\agent-project\tools\node\node.exe`；vitest 用 `仓库\node_modules\.bin\vitest.cmd run`
> - C++: `C:\Python314\python.exe -m ziglang c++ -w -std=c++17`（zig 内置 clang，首次编译约 1 分钟）
>
> 基线（2026-08-21 实测）：
> | 仓库 | 领域 | 基线 |
> |---|---|---|
> | sqlitedict | Py / 嵌入式 KV 数据库 | 87 passed（TablenamesTest 2 条 Windows 文件锁 flake 已 deselect） |
> | boltons | Py / 通用工具库 | 472 passed |
> | pathe | TS / 路径工具 | vitest 467 passed |
> | scule | TS / 对象处理 | vitest 63 passed |
> | cxxopts | C++ / 命令行解析 | 编译+运行链路通 |

---

## B-01 · sqlitedict（Py/数据库）：抽序列化层

```
目标仓库 D:\agent-project\target-repos\sqlitedict（嵌入式 KV 数据库，单文件核心 sqlitedict.py）。
任务：把 sqlitedict.py 里 encode/decode（序列化）相关的内联逻辑抽取为独立模块 serialize.py，
在 sqlitedict.py 中改为 import 复用；行为不变；并为 serialize 模块新增独立测试文件，
覆盖：pickle 协议往返、自定义 encode/decode 注入。

验收：
!cd /d D:\agent-project\target-repos\sqlitedict && C:\Python314\python.exe -m pytest tests -q --deselect tests/test_core.py::TablenamesTest

约束：所有 python 命令用 C:\Python314\python.exe 全路径；
不许改动公开 API；既有 87 条测试必须全绿（TablenamesTest 两条是已知 Windows flake，已排除）。
```

## B-02 · boltons（Py/工具库）：跨模块复用

```
目标仓库 D:\agent-project\target-repos\boltons（通用工具库，472 条测试全绿）。
任务：
1. 在 boltons/strutils.py 给 slugify 增加 max_length 参数（截断且不截断单词中间）；
2. 检查 boltons/urlutils.py 中是否有与 slugify 同模式的规范化逻辑，若有则改为调用 strutils 实现；
3. tests/ 新增用例：max_length 边界（0、恰等于词边界、超长）。

验收：
!cd /d D:\agent-project\target-repos\boltons && C:\Python314\python.exe -m pytest tests -q

约束：python 全路径；472 条基线全绿；若 urlutils 无同模式逻辑，在产物文档说明证据（引用具体行号）。
```

## B-03 · pathe（TS/路径工具）：新增跨平台工具函数

```
目标仓库 D:\agent-project\target-repos\pathe（TypeScript 路径工具库，vitest 467 passed）。
任务：新增一个 isSubpath(parent, child) 导出函数：判断 child 是否位于 parent 之内
（先 resolve 归一化，再比较，要求跨平台分隔符都能工作）；在 src 合适位置实现并导出；
test/ 新增 vitest 用例覆盖：嵌套 true、兄弟目录 false、相对/绝对混合、尾部斜杠。

验收：
!cd /d D:\agent-project\target-repos\pathe && node_modules\.bin\vitest.cmd run

约束：Node 在 D:\agent-project\tools\node\（直接调 node_modules\.bin\vitest.cmd 即可）；
既有 467 条全绿；代码风格对齐仓库（TS、无分号派系以仓库现状为准）。
```

## B-04 · scule（TS/对象处理）：新增大小写转换器

```
目标仓库 D:\agent-project\target-repos\scule（TypeScript 对象键转换库，vitest 63 passed）。
任务：新增 camelToConstantCase（如 "fooBar" → "FOO_BAR"），并让它接入既有的
转换体系（参照 pascalCase/camelCase 的既有实现方式）；test/ 新增用例：
常规驼峰、连续大写（如 "HTTPServer"）、含数字、空字符串。

验收：
!cd /d D:\agent-project\target-repos\scule && node_modules\.bin\vitest.cmd run

约束：Node 在 D:\agent-project\tools\node\；既有 63 条全绿；
实现方式必须复用仓库既有的内部辅助（若有），禁止另起炉灶复制逻辑。
```

## B-05 · cxxopts（C++/CLI 解析）：写一个真实可用的 CLI 工具

```
目标仓库 D:\agent-project\target-repos\cxxopts（header-only C++ 命令行解析库）。
任务：用该库编写一个文件行数统计 CLI（count_lines）：
支持 --dir 指定目录、--ext 指定扩展名（如 .md）、--json 切换 JSON 输出；
递归统计匹配文件的总行数；写一个 run_tests.cpp 测试程序，用 assert 验证
参数解析（默认值、显式值、未知参数报错）三类行为。

验收（编译+运行都必须退出码 0）：
!cd /d D:\agent-project\target-repos\cxxopts && C:\Python314\python.exe -m ziglang c++ -w -std=c++17 -I include run_tests.cpp -o run_tests.exe && run_tests.exe
!cd /d D:\agent-project\target-repos\cxxopts && C:\Python314\python.exe -m ziglang c++ -w -std=c++17 -I include count_lines.cpp -o count_lines.exe && count_lines.exe --dir . --ext .hpp

约束：编译器是 C:\Python314\python.exe -m ziglang c++（内置 clang），必须加 -w 屏蔽警告刷屏；
源文件写在仓库根目录；zig 首次编译较慢属正常。
```

---

## 执行记录

| 题号 | run | 架构师定级 | 实际复杂度匹配? | 成本 | 验收 | 备注 |
|---|---|---|---|---|---|---|
| B-01 | 20260821-131119 | M（拆 2 卡） | ✅ 合理 | ¥0.3939 | 人工通过 | 验收时撞上 Windows 文件锁 flake + 中间态，人工核实 87 全绿后 resolve 结案——处置台首次实战 |
| B-02 | 20260821-145141 | M | ✅ 合理 | ¥0.8314 | 483 passed 全绿 | urlutils 无同模式逻辑，工程师引行号举证，未强行改 |
| B-03 | 20260821-145908 | **S** | ⚠️ 偏低（预期 M） | ¥0.3520 | 474 passed 全绿 | 单函数+测试被判 S，理由自洽但与 B-04 口径不一 |
| B-04 | 20260821-150444 | M | ✅ 合理 | ¥0.2390 | 71 passed 全绿 | 与 B-03 同为"单函数+测试"却判 M——定级口径漂移点 |
| B-05 | 20260821-150934 | M | ⚠️ 偏低（预期 L） | ¥0.3015 | 编译+运行全过 | 陌生语言 C++ 双编译验收，CLI 产物真实可用（实测统计 21123 行） |

## 盲测结论（定级元能力）

- **5 单全部交付成功**，总成本 ¥2.12，平均 ¥0.42/单
- 定级准确度：3/5 完全合理；2/5 存在口径漂移（B-03 vs B-04 同构不同级）
- 改进方向：在架构师提示词中补"同构任务同级"一致性约束，或引入"定级参照样例"（few-shot 锚点）
