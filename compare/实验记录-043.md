# 实验记录 043 ｜ L-code 第三单：FastAPI-Vue-OAuth2 全栈 RBAC 改造（n=3 定稿单）

日期：2026-08-25 ｜ run：`architect-engineer/runs/20260825-164758` ｜ 性质：L-code 基线样本（n=3，冲定稿门槛）

## 任务

jason810496/FastAPI-Vue-OAuth2（42★，FastAPI+Vue3+JWT 骨架，User 无角色字段、默认 asyncpg/PostgreSQL）
→ 全栈 RBAC 改造：User 加 role（user/admin）、三个仅管理员的用户管理接口（列表/删除/改角色，
403/401 分流）、SQLite 离线化（零新依赖优先）、pytest 全量测试、verify_rbac.py 全链路脚本、
前端管理员入口按角色显隐 + requiresAdmin 路由守卫、集成卡只验不改。

拆卡：plan-only → 审卡零阻断放行 → 4 卡（侦察 / 后端 25 轮 / 前端 20 轮 / 集成 25 轮）。

## 结果

| 指标 | 数值 |
|---|---|
| 验收 | 4/4 pass（t2 一次 budget_exhausted 返工） |
| 独立复验 | pytest 5 passed ✓；verify_rbac.py 全链路 exit 0 ✓；check_frontend.py ALL PASS ✓；verify_integration.py exit 0 ✓ |
| 产物抽查 | router requiresAdmin meta + beforeEach 守卫 ✓；NavBar `v-if="role==='admin'"` ✓；npm build 降级合规（退出码+报错原文+豁免条款引用齐全）✓ |
| **成本** | **¥3.1396**（架构师 ¥0.2257 + 工程师 ¥2.9140，cost.jsonl 逐条入账） |

## 过程波折（如实记录）

1. **t2 首轮 25 轮预算耗尽**：async SQLAlchemy → SQLite 的离线化改造牵动
   模型/CRUD/API/测试/conftest 共 13 个文件，工作量超 25 轮预估；架构师给返工轮续跑后通过。
2. **verify_rbac.py 幂等性缺陷（系统自愈案例）**：首轮脚本无数据清理，
   重跑时 admin_user 撞唯一约束、注册断言必败。我在跑批中只做只读诊断（未干预），
   返工轮工程师自行发现并修复（复跑两次均 exit 0 证实幂等）。
3. **npm build 按预期豁免**：靶仓无 node_modules，集成卡在系统路径找到 npm.cmd 尝试构建、
   退出码 1、报错原文落档，严格走卡片第 2 条离线豁免条款——验收纪律没有走样。

## 成本归因与区间研判

t2 的 25 轮耗尽 + 返工是 ¥3.14 的主因：SQLite 化改造的实际复杂度（异步改同步、
依赖分支决策、测试基座重建）接近一次小型迁移，超出 L-code 前两单的"加功能/库内重构"量级。

**L-code 子类现状（n=3，达定稿门槛）**

| 样本 | 题型 | 成本 |
|---|---|---|
| 038（flask-vue-crud 加 rating） | 全栈加功能 | ¥1.1386 |
| 039（boltons 重构 + 472 用例全绿） | 库内重构 | ≈¥1.26 |
| **043（FastAPI-Vue-OAuth2 RBAC + 离线迁移）** | 全栈改造+迁移 | **¥3.1396** |

**L-code 区间定稿：¥1.14 ~ ¥3.14，均值 ≈ ¥1.85（n=3）**。
观察：成本与"是否涉及存量架构迁移"强相关——纯增量任务落在 ¥1.1~1.3，
带迁移性质的任务上探 ¥3+。对外口径建议加注此条，避免区间被误读为噪声大。

## 对六宫格的影响

- 「L · 自主拟卡」格子 read/code 双达标（read ¥1.78~4.06 / code ¥1.14~3.14）；
- 剩余窟窿：L/M 人工拟卡各差 1 单（P1，加固规划溢价结论用）。
