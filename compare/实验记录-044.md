# 实验记录 044 ｜ M · 人工拟卡第二单：RFC 6901 JSON Pointer 求值器

日期：2026-08-26 ｜ run：`architect-engineer/runs/20260826-093018` ｜ 性质：M·人工拟卡基线样本（n=2）

## 任务

人工任务书（含结构设计与验收设计）：RFC 6901 JSON Pointer 求值器，
纯标准库 `resolve()` + 自定义 `JSONPointerError`；判卷 oracle 为 RFC 6901
Section 5 的 12 条官方示例（含 `~0`/`~1` 转义、空键、空格键、含斜杠键）
+ 6 条错误处理（越界/前导零/`-` 令牌/键不存在/类型不匹配×2）。
专项杀点：`~01` 反转义顺序（先 `~1` 再 `~0`，RFC Section 4 原文顺序）。

单卡，预算 20 轮。卡盒：`runs/manual-20260826-093500/tasks/`。

## 结果

| 指标 | 数值 |
|---|---|
| 验收 | 1/1 pass（一次返工：首轮验证器未捕获真实 stdout，返工轮修复） |
| 独立复验 | `python test_jsonpointer.py`：26 tests OK exit 0 ✓；`python verify_jsonpointer.py`：ALL PASS exit 0，`~01 → ~1` 专项用例通过 ✓ |
| **成本** | **¥0.1146**（架构师 ¥0.0278 + 工程师 ¥0.0868，11 次调用） |
| 耗时 | 单窗口跑完，无需 resume |

## 观察

1. 返工原因值得记档：首轮工程师的验证脚本未捕获真实 stdout/退出码，
   架构师验收打回——"验收证据保真"纪律（036 起）在 M 级单卡场景同样生效。
2. 成本 ¥0.11 显著低于 029（¥0.27）：题目边界更窄（单文件库 vs 附件解析+双交付物），
   且 unittest 零依赖省去环境折腾。M·人工区间两单拉开属正常。

## M · 人工拟卡格子现状（n=2）

| 样本 | 题型 | 成本 |
|---|---|---|
| 029（GAIA 附件解析+双交付物） | 数据解析 | ¥0.2670 |
| **044（RFC 6901 求值器+官方判卷）** | 规范实现 | **¥0.1146** |

M·人工区间暂记 **¥0.11 ~ ¥0.27**（n=2），均 ≈¥0.19。
对照 M·自主（n=41，均 ¥0.468）：人工拟卡的"规划溢价"在 M 级依然显著，
但 044 与 029 题型差异较大，区间置信度 ★★ 中偏薄——若要对外报价建议补至 n=3。
