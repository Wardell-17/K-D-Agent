# 实验记录 026 ｜ 返工轮成本漏点修复（预算不再减半 + 返工纪律注入）

**日期**：2026-08-21 ｜ **动机**：025 失败解剖实锤——返工轮全新上下文重读仓库 +
预算减半（15→7→4）等于判死刑，write_file 还没轮到预算就没了

## 改动（两处，互为一体）

1. **`orchestrator.py` 返工分支**：删除 `remaining_budget //= 2`——成本控制由
   "返工次数上限"（escalation.max_verify_failures）承担，不再拿预算开刀；
   同时向 confirmed_facts 注入「返工纪律」：禁止从零重新探索
   （list_dir/read_file ≤3 次），先读卡片「返工与备注」与已有产物，直奔修复。
2. **`prompts/engineer.md` 新增第 7 条**：返工轮纪律进系统提示词（双保险：
   移交包注入是"本轮事实"，提示词是"岗位性格"）。

## 验证经过（如实记录：三次陷阱卡均未逼出返工）

| 尝试 | 设计 | 结果 |
|---|---|---|
| v1 | 验收要求 report.md 含 "3 fruits"（goal 不提） | 工程师首轮主动写报告顺手满足，¥0.020 |
| v2 | 验收要求 verdict.txt == "PASS-026"（不可猜） | **工程师直接从验收标准里读到要求并照做**，¥0.018 |
| v3 | 预算压到 4 轮 | 工程师 4 轮内完成全部写入，预算耗尽但产物齐、验收过，¥0.012 |

**意外发现（比原命题更重要）**：移交包的 acceptance 对工程师完全可见，
工程师会直接"照着验收标准答题"。这是双刃剑——
好处：首轮通过率高（三次陷阱全部首轮过）；风险：若验收标准写得窄，
工程师会"应试"而非"做好"。**验收标准的质量就是这个系统的天花板**，
写卡纪律（5.2 节）的地位再升一级。

## 结论

- 机制修复已实装（静态可审计：orchestrator.py 返工分支 + engineer.md 第 7 条）
- 行为级 A/B 验证**挂起**：人工陷阱在"验收可见"前提下构造不出自然返工，
  等待下一个真实返工场景自动验证（届时对比返工轮工具调用分布，
  预期 read_file/list_dir 占比从 025 的 ~40% 降到 ≤3 次/轮）
- 三次陷阱卡共 ¥0.05，换来"验收可见性"这个认知，值

## 附：缓存命中率分析（与 Gemini 讨论后的账本实测，2058 次工程师调用）

- token 加权命中率 80.7%；对话早期 74.7% vs 中后段 76.7%——**长对话并没有更高**，
  说明瓶颈不是"前缀被弄脏"，而是**每轮新增未缓存增量太大**（read_file 单页
  2 万字符 ≈5k tokens 永远是 Miss）。Harness 的 95%+ 来自"巨型静态前缀 + 极小增量"
  的测评形态，与 Agentic 循环是数学结构差异，不是工程失误。
- 排查确认：静态前置（engineer.md 锁 system）与动态沉底（状态栏追加末尾）本就合规。
- 抓到真漏点并修复：移交包 JSON 内嵌 `remaining_budget` 每轮递减、击穿前缀缓存，
  已剔除（预算展示由末尾状态栏承担，packet 前缀绝对静态）。
- 结论：80% 接近结构天花板，不值得为追 95% 牺牲深读能力；总 token 才是成本主战场。
