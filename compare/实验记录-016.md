# 实验记录 016 —— 极限深读压测：逆向梳理飞控仿真仓库（budget 字段首航）

- 日期：2026-08-19
- 实验人：Wardell（命题：从「写小脚本的实习生」进化为「独立接管几万行代码调研的高级研发专家」）
- 关联：015（真实任务压测、暴露预算不足）；budget 卡级字段刚上线即首航

## 考题

单卡、25 轮预算：工程师（DeepSeek V4-Flash）独立深读开源四旋翼仿真控制仓库
Quadcopter_SimCon（GitHub 浅克隆，31 个 Python 文件，含非线性动力学 + 串级 PI 控制），
逆向输出控制链路结构文档 control_architecture.md。

卡盒：`compare/exp016-stress-card.md`（budget: 25）。
验收含一道硬闸门：文档引用的每个仓库内源码路径，用脚本逐一验证真实存在。

## 结果：一次通过，且远未用尽预算

| 指标 | 数值 |
|---|---|
| 工程师 LLM 调用 | 11 次（25 轮预算只用了不到一半） |
| tokens | 174,976 |
| 成本 | **¥0.1985** |
| 耗时 | 约 2 分钟 |
| 验收 | 4 道命令 + 架构师审查全部通过，零返工 |

## 产物质量（人工抽查源码对账）

工程师的探索策略完全按卡的指引：先 list_dir 摸结构，再按相关性读了约 10 个核心文件
（quad.py / ctrl.py / initQuad.py / mixer.py / trajectory.py / run_3D_simulation.py 等），
没有平均用力。

人工对源码抽查其三处核心结论，全部属实：

1. **动力学核心**：`quad.py::Quadcopter.state_dot` 确在 71-224 行区间（forces/state_dot/update
   方法与报告描述一致），21 维状态方程、ode(dopri5) 积分属实；
2. **串级结构**：`ctrl.py::Control` 确含 position/velocity/attitude/rate 各级方法，
   报告梳理的「位置环→速度环→期望姿态解算→姿态环→角速度环→mixer」级联与源码一致；
3. **增益定义**：pos/vel/att/rate 的 P/D/I gain 确实全部定义于 ctrl.py 顶部。

工程师还主动标注了两处「未读懂」（qd_mix 偏航权重混合的几何细节、trajectory 多项式
系数矩阵）——诚实性纪律在无人监督的单卡任务中同样生效。

产物：`compare/exp016-deliverable-control_architecture.md`（11.8KB）。

## 结论

- **budget 卡级字段首航成功**：深读任务的预算瓶颈被制度性解除（虽然本次 11 次调用
  就完成，说明对几万行以内的仓库，25 轮是充裕的安全边际而非必需品）。
- **深读类任务的验收范式确立**：硬闸门（路径逐一验证存在）+ 架构师审结构 +
  人工抽查对账，三层取证相互独立。
- 系统的「高级研发专家」能力得到第一次实锤证据：独立遍历陌生仓库 → 定位核心
  数学模型 → 逆向控制逻辑 → 输出可核查文档，全程 ¥0.20。

## 复现方式

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File D:\agent-project\compare\run-mvp-exp014.ps1 -CardPath D:\agent-project\compare\exp016-stress-card.md
```
