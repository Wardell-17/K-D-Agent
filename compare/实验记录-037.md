# 实验记录 037 ｜ 036 治理判决：落地验收纪律后的自主拟卡复跑

日期：2026-08-25 ｜ run：`architect-engineer/runs/20260825-105950` ｜ 性质：治理有效性验证（036 的对照复跑）

## 设计

与 036 完全同题（自主拟卡拆卡的 L-02），唯一变量是 036 治理已入库：
验收命令落地纪律（禁占位符/未验证路径、入口自发现、陌生仓库先侦察卡）
+ 路径隔离纪律（workspace = 本次 run 目录）。
流程采用 plan-only → 人工审卡（修正了 4 张卡的越界路径）→ `--cards` 放行。

## 拆卡质量变化（治理前 → 治理后）

- t2 后端：全部入口自发现（`cd 仓库根 && .venv\Scripts\python.exe -m pytest`，
  测试文件 glob 定位），零硬编码路径。
- t3 前端：验收命令在执行时读取侦察卡产出的 `_recon/manifest.json`
  （frontend_dir / book_form_component），实现"侦察产物运行时回填"——
  架构师自发演化出两阶段规划，超出纪律文本的预期。

## 结果

| 维度 | 036（治理前） | 037（治理后） |
|---|---|---|
| 验收 | 1/4 pass，3 卡升级/冻结 | **4/4 全部一轮 pass，零返工** |
| 成本 | ¥1.5080 | **¥0.7087**（含 plan-only 拆卡 ¥0.075） |
| 证据 | 验收命令执行失败（路径/占位符） | 独立复跑确认：集成 14/14 PASS、pytest 15 passed |

成本口径备注：执行段曾被沙盒杀掉一次后台进程（该段 token 消耗随缓冲丢失、未入账），
实际总成本略高于 ¥0.7087；最终以 `--resume` 前台续跑完成，入账部分完整可审计。
即便如此，相对 036 的 ¥1.5080 仍是 >2 倍的成本下降，且结果从 3/4 升级翻转为全绿。

## 结论

- 036 治理有效：**"验收命令必须落地"这一条纪律，直接把自主拟卡模式从不可用拉回可用**。
- 规划质量是最大成本杠杆的论断（036）获得第二次证实：同题同系统，成本 1.51 → 0.71。
- 流程资产确认：`plan-only → 人工审卡 → --cards 放行` 是重型任务的标准姿势，
  本次人工审卡仅改路径一处即放行，审卡成本约 1 分钟。
- 已知残留：后台 nohup 跑批在本沙盒会被静默杀掉且日志缓冲丢失，
  重型跑批应以前台分段 + `--resume` 执行（与 kd_dispatch 失效同源）。
