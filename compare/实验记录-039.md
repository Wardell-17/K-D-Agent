# 实验记录 039 ｜ L-03：boltons 跨模块重构 + 472 用例全绿约束

日期：2026-08-25 ｜ run：`architect-engineer/runs/20260825-141537` ｜ 性质：L 级基线样本（第 3 单）

## 任务

mahmoud/boltons（6917★，纯标准库）：iterutils.py 约 10 对 eager/lazy 孪生函数的
"消费迭代器并收集结果"样板去重——eager 函数改薄封装，公共 API 签名与行为零变化，
全量 pytest（472 用例）保持全绿，量化重复量对比（脚本产出，不接受口头估计）。

流程：plan-only → 人工审卡 → `--cards` 放行（审卡逮到一处纪律漏用，见下）。

## 审卡记录（审批门价值的又一实证）

架构师拆卡整体优秀（侦察基线 → 重构 → 量化证据 3 卡，AST 公开函数存在性检查、
api_compat_check 逐签名比对、dup_stats 量化、"通过数与基线一致（用户声称 472）"
的诚实口径），但**漏用 035 解释器口径纪律**（验收命令全用裸 python，pytest 需装包）。
人工审卡 3 处修正（强制 .venv + 锁解释器路径）后放行，审卡耗时约 2 分钟。
→ 纪律库越大，架构师漏用单条纪律的概率越高，审批门兜底的边际价值在上升。

## 结果

| 指标 | 数值 |
|---|---|
| 验收 | 3/3 pass（t3 流程收尾缺失但产物达标，budget_exhausted 后判 pass） |
| 返工 | 0 次 |
| **成本** | **¥1.2638**（架构师 ¥0.1681 + 工程师 ¥1.0957） |
| 独立复验 | pytest **472 passed**（与基线一致）；api_compat PASS（20 函数签名逐一相等）；dup_stats：样板语句 10→0、重复收集块 9→0、eager 体总行数 17→14 |

## L 桶现有样本（3 单）

| 样本 | 任务类型 | 成本 | 验收 |
|---|---|---|---|
| L-01 | 飞控仓库逆向（深读+文档） | 见 017 记录 | pass |
| L-02 | 全栈端到端加字段（038 干净单） | ¥1.1386 | 4/4 |
| L-03 | 跨模块重构+全量测试约束 | ¥1.2638 | 3/3 |

L 桶成本区间暂稳在 ¥1.1~1.3（自主拟卡口径），与 M 桶（≈¥0.3）拉开清晰档次。

## 观察

- t3（文档+统计双交付物卡）15 轮预算耗尽——与 029"附件解析双交付物"模式同构，
  再次印证"双交付物卡预算应上调半档"的既有结论（已写入拆卡纪律，样本 +1）。
- boltons 纯标准库 + venv 纪律下，全程零环境噪音，成本全部花在思考与代码上，
  是理想的基线样本类型。
