# 决策框架：这套双模型 Agent 系统到底该怎么用、怎么评

> 写给自己的一页纸。原则：**先定判据，再跑实验，最后下结论。**顺序不能反。

---

## 0. 我们究竟在回答什么问题？

整个项目归根结底只有三个问题，所有行动都应该能映射到其中之一：

| 编号 | 问题 | 性质 |
|---|---|---|
| Q1 | 双模型路由（贵思考 + 便宜执行）是不是比单模型划算？ | **假设验证** —— 靠实验回答 |
| Q2 | 日常干活该用自建 MVP 还是 Harness？ | **工具选型** —— 靠判据 + 场景回答 |
| Q3 | 我能不能向别人（面试官）讲清楚这套系统为什么是这样设计的？ | **理解内化** —— 靠复盘和实战回答 |

之前容易混乱的根源：把三个问题混在一场对话里同时追。以后每做一件事，先问"这回答的是 Q 几"。

---

## 1. Q1 的判据：什么叫"划算"

双模型路由赢，需要**同时**满足：

1. **质量不降**：架构师验收通过率 ≥ 单模型直接做的基线
   *（为什么：便宜模型执行如果错误率高，返工会吃掉省下的钱）*
2. **成本显著低**：总成本 < 全用贵模型的 50%
   *（为什么：工程师任务占 token 大头，实验 001 里工程师 26,591 tokens vs 架构师 2,044 tokens，比例 13:1，理论上成立，但要在多任务上复验）*
3. **延迟可接受**：总耗时 < 单模型的 2 倍
   *（为什么：多一圈派工/验收的通信开销是固有成本）*

判定所需的最小实验量：**≥ 10 个不同类型任务**（脚本类 / 数据处理类 / 文档类 / 调试类），每类至少 2 个。样本量 1 不能下任何结论。

---

## 2. Q2 的判据：什么场景用哪条线

| 场景 | 用哪条线 | 为什么 |
|---|---|---|
| 需要精确报账（给别人看成本、算 ROI） | **MVP** | Harness 拿不到 token 用量，MVP 自己记账 |
| 需要可审计、可复现（实验记录） | **MVP** | 验收逻辑是自己写的，每个判定有 report.json 证据 |
| 日常快速干活、要 UI、不想碰终端 | **Harness** | Web 界面 + 成熟工具链 + 会话持久化 |
| 任务复杂、需要读大量文件/搜索/多轮工具 | **Harness** | 它的工具生态（fs/search/shell/skill）比 MVP 的裸 subprocess 强得多 |
| 想改编排逻辑本身（比如加第三个角色"测试员"） | **MVP** | 改自己的 370 行 Python 比理解 Harness 的 cordis 层叠配置快 |
| 简历/面试展示 | **两个都要** | MVP 讲"从零设计"，Harness 讲"工程评估与移植"，对照讲才有深度 |

一句话记忆：**MVP 是实验室，Harness 是车间。** 新想法在实验室验证，验证过的流程放到车间量产。

---

## 3. Q3 的判据：什么叫"真懂了"

能不看任何笔记回答出以下问题，才算内化：

1. 为什么架构师用贵模型、工程师用便宜模型，而不是反过来？
   *（提示：错误在哪一环的代价更大？哪一环的 token 占比大？）*
2. 为什么要有"验收"环节，而不是工程师说完了就直接交付？
   *（提示：LLM 的自汇报可信吗？实验里 engineer finished 但预算耗尽的案例）*
3. 为什么 Harness 里 K3 一开始报 400？你是怎么定位的？
   *（提示：developer 角色、reasoning 标记、源码第 788 行）*
4. 如果任务非常简单（一句话能做完），走编排系统划算吗？为什么？
   *（提示：overhead——规划+验收本身也要花 token）*
5. 多智能体系统和"一个提示词写得很好的单智能体"的本质区别是什么？
   *（提示：上下文隔离、角色专精、可独立替换模型——这是书第 2、6 章的核心）*

答不上来的就是补课清单。补课材料：`D:\agent-project\ai-agent-book\`（《深入理解 AI Agent》对应代码仓库）。

---

## 4. 待补的实验（按优先级）

| # | 实验 | 回答什么 | 状态 |
|---|---|---|---|
| E1 | 同一任务全用 K3 单模型跑，记录成本和质量 | Q1 的基线（没有基线，"省钱"无从谈起） | 未做 |
| E2 | 5 个不同类型任务在两条线上各跑一遍 | Q1 的统计意义 + Q2 的体感差异 | 未做 |
| E3 | 给 Harness 的 DeepSeek 侧单独算账（工程师 API 是按 token 计费的，这部分能算） | 补齐成本口径 | 未做 |
| E4 | 故意给一个会失败的任务（如"读取不存在的 API"），看两条线的错误处理差异 | 鲁棒性对比 | 未做 |

---

## 5. 下一步：GitHub 实战（真正的逻辑闭环）

"逻辑闭环"的完整含义：**不是做出一个工具，而是走完"问题 → 假设 → 设计 → 实现 → 验证 → 复盘 → 改进"整个循环，并且每一步你都能说出为什么。**

实战方式：去 GitHub 找一个真实的开源 multi-agent 项目（如著名的几个编排框架），做三件事：

1. **读**：它的架构师/工程师角色怎么分的？和我们的设计差在哪？
2. **比**：它解决过什么我们没想到的问题？（比如子代理超时、上下文爆炸、死循环派工）
3. **改**：给它提一个小改进或修一个小 bug——哪怕只是文档。这是从"使用者"变成"参与者"的分界线，也是简历上从"做过项目"变成"参与过开源"的分界线。

---

*框架版本 v1 ｜ 2026-08-14 ｜ 每次实验后回来更新判据和结论*
